import React, {useState,useContext} from 'react'; import { AuthContext } from './AuthContext';
export default function Login(){ const { login } = useContext(AuthContext); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [err,setErr]=useState('');
const submit=async(e)=>{ e.preventDefault(); try{ const base=(import.meta.env.MODE==='development'?'http://localhost:8000':''); const res=await fetch(base + '/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}); const j=await res.json(); if(!res.ok){ setErr(j.error||'Login failed'); return; } login(j.token,j.user); window.location.href='/'; }catch(e){ setErr(String(e)); } };
return (<div className='max-w-md mx-auto card'><h2 className='text-xl font-bold mb-3'>Login</h2>{err && <div className='text-red-600 mb-2'>{err}</div>}<form onSubmit={submit} className='space-y-3'><input value={email} onChange={e=>setEmail(e.target.value)} placeholder='Email' className='w-full p-2 border rounded'/><input type='password' value={password} onChange={e=>setPassword(e.target.value)} placeholder='Password' className='w-full p-2 border rounded'/><button className='w-full bg-green-600 text-white p-2 rounded'>Login</button></form></div>); }
